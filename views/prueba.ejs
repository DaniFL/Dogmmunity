<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TikTok Simulation</title>
    <style>
        body {
            margin: 0;
            overflow: hidden;
        }

        .container {
            height: 100vh;
            display: flex;
            flex-direction: column;
            overflow-y: scroll;
            scroll-snap-type: y mandatory;
        }

        .video-container {
            height: 100vh;
            scroll-snap-align: start;
            position: relative;
        }

        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .controls {
            position: absolute;
            right: 10px;
            bottom: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .control-button {
            background: rgba(0, 0, 0, 0.6);
            color: white;
            border: none;
            border-radius: 50%;
            padding: 10px;
            margin: 5px;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="video-container">
            <video src="/media/dog_video1.mp4" muted></video>
            <div class="controls">
                <button class="control-button">❤️</button>
                <button class="control-button">💬</button>
                <button class="control-button">🔄</button>
            </div>
        </div>
        <div class="video-container">
            <video src="/media/dog_video2.mp4" muted></video>
            <div class="controls">
                <button class="control-button">❤️</button>
                <button class="control-button">💬</button>
                <button class="control-button">🔄</button>
            </div>
        </div>
    </div>
    <!-- Bootstrap JS -->
    <script src="../public/js/prueba.js"></script>
</body>

</html>