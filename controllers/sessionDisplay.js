/*document.addEventListener('DOMContentLoaded', () => {
    // Obtener el nombre de usuario desde un elemento oculto en el HTML
    const usernameElement = document.getElementById('username').value;
    
    // Verifica si el elemento existe y tiene contenido
    if (usernameElement) {
        const username = usernameElement.innerText;

        // Mostrar el mensaje de bienvenida
        const welcomeMessage = document.getElementById('welcome-message');
        welcomeMessage.innerText = '¡Hola, ${username}!';
    } else {
        console.log('Usuario no encontrado en la sesión.');
    }
});
*/